on: push
jobs:
 u:
  runs-on: ubuntu-latest
  permissions:
   contents: write
  env:
   GH_TOKEN: ${{secrets.GITHUB_TOKEN}}
  steps:
  - uses: actions/checkout@v4
  - run: gh release create latest || true
  - run: gh release upload latest start.py --clobber